<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <a href="/"><img src="/img/logo-DH.png" alt="Logo Digital House"></a>
    </figure>
    <h1><%= movie.title %> </h1>    
    <br>
    <p>RATING: <%= movie.rating %> </p>
    <p>AWARDS: <%= movie.awards %> </p>
    <p>LENGTH: <%= movie.length + ' min'%> </p>
    <p>RELEASE DATE: <%= moment(movie.release_date).format('YYYY-MM-DD') %> </p>
    <br>
    <h4>Género: <a href="/genres/detail/<%= movie.genre.id %>"><%= movie.genre.name %></a></h4>
    <br>
    <section>
        <!--Aquí debe colocar los botones de Modificar - Borrar y Listado de Películas-->
        <a class="botonModificar" href="/movies/edit/<%= movie.id %>">Editar</a>
        <a class="botonBorrar" href="/movies/delete?id=<%= movie.id %>&title=<%= movie.title %>">Borrar</a>
    </section>
    <br>
    <br>
    <p><a class="botonVolver" href="/movies">Volver</a></p>

</body>
</html>